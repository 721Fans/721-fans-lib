export interface ChallengeData {
  iss: string; // issuer domain
  addr: string; // eth address
  chan: string; // social media channel
  cbk: string; // callback url
  uuid: string; // user identifier
}